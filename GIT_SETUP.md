# Настройка для публикации в Git

## ✅ Что было сделано

### 1. Обновлен `.gitignore`
Добавлены файлы с личной информацией, которые **НЕ будут** попадать в репозиторий:
- `DEPLOYMENT_NO_DOCKER.md` - содержит IP адрес вашего сервера
- `ecosystem.config.js` - содержит пути к вашему серверу
- `PROJECT_QUALITY_REPORT.md` - отчет о качестве проекта

### 2. Созданы публичные версии (примеры)
Созданы файлы-примеры **БЕЗ личной информации**, которые **МОЖНО** публиковать:
- `DEPLOYMENT_NO_DOCKER.example.md` - инструкция с placeholder'ами (your-user, your-server-ip)
- `ecosystem.config.example.js` - конфигурация PM2 с placeholder'ами

### 3. Обновлен README.md
Ссылки в README теперь указывают на публичные версии файлов.

---

## 📋 Что будет в репозитории

### ✅ Будет опубликовано:
- ✅ `README.md` - основная документация
- ✅ `DEPLOYMENT.md` - общая инструкция по развертыванию (без личной информации)
- ✅ `DEPLOYMENT_NO_DOCKER.example.md` - пример инструкции без Docker
- ✅ `ecosystem.config.example.js` - пример конфигурации PM2
- ✅ `env.example` - шаблон переменных окружения
- ✅ Весь код проекта (backend/, frontend/, nginx/)
- ✅ Все остальные документы (PROMPTVAULT_SPEC.md, чеклисты спринтов и т.д.)

### ❌ НЕ будет опубликовано:
- ❌ `DEPLOYMENT_NO_DOCKER.md` - ваша личная версия с IP адресом
- ❌ `ecosystem.config.js` - ваша личная конфигурация с путями
- ❌ `PROJECT_QUALITY_REPORT.md` - отчет о качестве
- ❌ `.env` - переменные окружения (уже в .gitignore)
- ❌ База данных и логи (уже в .gitignore)

---

## 🚀 Как опубликовать

### 1. Проверьте, что все правильно

```bash
# Проверить, какие файлы будут добавлены в Git
git status

# Убедитесь, что файлы с личной информацией НЕ в списке:
# - DEPLOYMENT_NO_DOCKER.md
# - ecosystem.config.js
# - PROJECT_QUALITY_REPORT.md
```

### 2. Добавьте файлы в Git

```bash
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Проверить, что добавлено
git status
```

### 3. Создайте коммит

```bash
git commit -m "Pervaya versiya PromptVault - polnaya realizaciya"
```

### 4. Опубликуйте в репозиторий

```bash
# Если репозиторий еще не создан на GitHub
git remote add origin git@github.com:your-username/promptvault.git

# Опубликовать
git push -u origin main
```

---

## ⚠️ Важно

1. **Никогда не коммитьте** файлы с личной информацией:
   - IP адреса серверов
   - Пути к вашим проектам
   - Токены и секреты
   - Личные данные

2. **Всегда используйте** `.env` для секретов (уже в .gitignore)

3. **Публикуйте только** примеры и шаблоны без личной информации

---

## 📝 Для будущих обновлений

При обновлении проекта:

1. **Локальные файлы** (с личной информацией) остаются на вашем компьютере
2. **Публичные версии** (примеры) можно обновлять и коммитить
3. **Код проекта** - можно свободно коммитить

Если нужно обновить примеры файлов:

```bash
# Обновить публичную версию на основе вашей локальной
# (удалив личную информацию)
# Затем:
git add DEPLOYMENT_NO_DOCKER.example.md
git commit -m "Obnovlen primer razvertyvaniya"
git push
```

---

## ✅ Готово!

Теперь вы можете безопасно опубликовать проект в открытый репозиторий. Вся личная информация останется только у вас локально.

