# Локальный запуск PromptVault

## 📋 Подготовка

### 1. Проверка окружения

**Python:**
```powershell
python --version
# Должно быть Python 3.11 или выше
```

**Node.js:**
```powershell
node --version
npm --version
```

### 2. Создание .env файла

Файл `.env` уже создан из `env.example`. Для локального запуска можно оставить значения по умолчанию (токены Telegram не обязательны для просмотра UI).

---

## 🚀 Запуск

### Шаг 1: Установка зависимостей Backend

```powershell
cd backend
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```

### Шаг 2: Инициализация базы данных

```powershell
# В папке backend (с активированным venv)
# Создать директорию для БД
mkdir -p data

# Применить миграции
alembic upgrade head

# Или инициализировать вручную
python scripts/init_db.py
```

### Шаг 3: Запуск Backend (в первом терминале)

```powershell
# В папке backend (с активированным venv)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Проверка:** Откройте http://localhost:8000/docs - должна открыться документация API

### Шаг 4: Установка зависимостей Frontend

```powershell
# В новом терминале, в корне проекта
cd frontend
npm install
```

### Шаг 5: Запуск Frontend (во втором терминале)

```powershell
# В папке frontend
npm run dev
```

**Проверка:** Откройте http://localhost:3000 - должна открыться главная страница

---

## 🎯 Что вы увидите

### Frontend (http://localhost:3000)
- Темная тема
- Заголовок "PromptVault"
- Поисковая строка
- Фильтры (теги, закрепленные)
- Список промптов (пока пустой, если БД пустая)

### Backend API (http://localhost:8000/docs)
- Swagger документация API
- Можно тестировать эндпоинты прямо в браузере

---

## 📝 Добавление тестовых данных

### Через API (Swagger UI)

1. Откройте http://localhost:8000/docs
2. Найдите эндпоинт `POST /api/v1/prompts`
3. Нажмите "Try it out"
4. Вставьте пример:

```json
{
  "tg_message_id": 1,
  "tg_channel_id": -1001234567890,
  "text": "Пример промпта для тестирования. Это тестовый промпт для проверки работы приложения.",
  "is_pinned": false
}
```

5. Нажмите "Execute"

**Примечание:** Для создания промпта через API нужен Bearer токен. В development режиме проверка может быть отключена, или используйте токен из `.env` (API_SECRET).

### Через Python скрипт

Создайте файл `backend/add_test_data.py`:

```python
from app.database import SessionLocal
from app.crud import prompt as crud_prompt
from app.schemas.prompt import PromptCreate

db = SessionLocal()

# Создать тестовый промпт
test_prompt = PromptCreate(
    tg_message_id=1,
    tg_channel_id=-1001234567890,
    text="Пример промпта для тестирования. Это тестовый промпт для проверки работы приложения.",
    is_pinned=False
)

crud_prompt.create_prompt(db, test_prompt)
print("Тестовый промпт создан!")

db.close()
```

Запустите:
```powershell
cd backend
.\venv\Scripts\activate
python add_test_data.py
```

---

## ⚠️ Возможные проблемы

### Backend не запускается

**Ошибка:** `ModuleNotFoundError`
```powershell
# Убедитесь, что venv активирован и зависимости установлены
.\venv\Scripts\activate
pip install -r requirements.txt
```

**Ошибка:** `alembic: command not found`
```powershell
# Установите зависимости
pip install -r requirements.txt
```

### Frontend не запускается

**Ошибка:** `npm: command not found`
- Установите Node.js с https://nodejs.org/

**Ошибка:** `Cannot find module`
```powershell
# Удалите node_modules и установите заново
rm -r node_modules
npm install
```

### БД не создается

```powershell
# Создайте директорию вручную
cd backend
mkdir data
# Затем примените миграции
alembic upgrade head
```

---

## 🛑 Остановка

1. В терминалах нажмите `Ctrl+C`
2. Деактивируйте venv (если нужно):
```powershell
deactivate
```

---

## 📊 Проверка работы

### Backend
- http://localhost:8000 - корневой эндпоинт
- http://localhost:8000/health - healthcheck
- http://localhost:8000/docs - Swagger UI

### Frontend
- http://localhost:3000 - главная страница

### API запросы
```powershell
# Получить список промптов
curl http://localhost:8000/api/v1/prompts

# Healthcheck
curl http://localhost:8000/health
```

---

## 🎉 Готово!

Теперь вы можете:
- Просматривать UI приложения
- Тестировать API через Swagger
- Добавлять тестовые данные
- Разрабатывать новые функции

